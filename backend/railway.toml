# Set service "Root Directory" to `backend` in the Railway dashboard.
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[build.buildArgs]
ANALYZER_TYPE = "mock"

[deploy]
healthcheckPath = "/ping"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
